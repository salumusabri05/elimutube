{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx prisma generate --schema=backend/api/prisma/schema.prisma && npm run build:backend"
  },
  "deploy": {
    "startCommand": "npm run start:backend:prod"
  }
}
